import { TngButtonDirective } from './tng-button.directive';

describe('TngButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new TngButtonDirective();
    expect(directive).toBeTruthy();
  });
});
